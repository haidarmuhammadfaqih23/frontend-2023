/**
 * usecase tampilkan 100 produk
 * - dari produk 1
 * - sampai produk 100
 * - naikkan produk 1 ke 2 dst
 *
 */

for (let p = 1; p <= 10; p++) {
  console.log("product ke: ", p);
}
console.log(`\n`);

for (let n = 1; n <= 10; n++) {
  console.log(`croduct ke: ${n}`);
}
console.log(`\n`);

for (let z = 1; z <= 10; z = z + 1) {
  console.log("zroduct ke: ", z);
}
